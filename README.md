# CMDR Helper Extension

A VS Code integration for the **CMDR CLI** toolkit. This extension brings powerful automation tools directly to your explorer context menu.

**Current Version:** `0.0.1` (Initial Release)

---

### Core Features

The extension currently provides 3 main tools accessible via the "CMDR Tools" menu:

1.  **RMC (Remove Comments)** – Instantly strips comments from a single file or an entire directory.
2.  **Tree to Clipboard** – Generates a directory structure tree and copies it to your clipboard (with exclusion support).
3.  **FC (Files Combine)** – Merges multiple files into a single document (optimized for Dart/Flutter projects).

---

### Requirements

* Requires **CMDR CLI** to be installed and available in your system PATH.